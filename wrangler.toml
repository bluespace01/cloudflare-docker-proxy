name = "cloudflare-docker-proxy"
compatibility_date = "2023-12-01"

[dev]
ip = "0.0.0.0"
port = 8787
local_protocol = "http"

[env.dev.vars]
MODE = "debug"
TARGET_UPSTREAM = "https://registry-1.docker.io"

[env.production]
name = "cloudflare-docker-proxy"
# routes = [
#   { pattern = "docker.bluespace.so", custom_domain = true },
#   { pattern = "quay.bluespace.so", custom_domain = true },
#   { pattern = "gcr.bluespace.so", custom_domain = true },
#   { pattern = "k8s-gcr.bluespace.so", custom_domain = true },
#   { pattern = "k8s.bluespace.so", custom_domain = true },
#   { pattern = "ghcr.bluespace.so", custom_domain = true },
#   { pattern = "cloudsmith.bluespace.so", custom_domain = true },
# ]

[env.production.vars]
MODE = "production"
TARGET_UPSTREAM = ""

[env.staging]
name = "cloudflare-docker-proxy-staging"
# route = { pattern = "docker-staging.bluespace.so", custom_domain = true }

[env.staging.vars]
MODE = "staging"
TARGET_UPSTREAM = ""
